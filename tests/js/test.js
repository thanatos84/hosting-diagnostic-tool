// test.js — This file is fetched by index.html to detect JS execution support
// If the server can serve this as a JS file and the browser executes it, this marker is set
window.__JS_SUPPORTED__ = true;
window.__JS_TIMING__ = Date.now();
